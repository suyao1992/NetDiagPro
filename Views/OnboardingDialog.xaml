<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="NetDiagPro.Views.OnboardingDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="欢迎"
    PrimaryButtonText="下一步"
    SecondaryButtonText="跳过"
    DefaultButton="Primary"
    PrimaryButtonClick="PrimaryButton_Click"
    SecondaryButtonClick="SecondaryButton_Click">

    <Grid Width="400" Height="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Icon -->
        <Border Grid.Row="0" Width="80" Height="80" CornerRadius="40"
                Background="{ThemeResource AccentFillColorDefaultBrush}"
                HorizontalAlignment="Center" Margin="0,16,0,16">
            <FontIcon x:Name="StepIcon" Glyph="&#xE8D7;" FontSize="36" 
                      Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>

        <!-- Content -->
        <StackPanel Grid.Row="1" Spacing="12" VerticalAlignment="Center">
            <TextBlock x:Name="StepTitle" Text="欢迎使用 NetDiag Pro" 
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       HorizontalAlignment="Center" TextAlignment="Center" />
            <TextBlock x:Name="StepDescription" 
                       Text="智能网络诊断管理平台" 
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" TextAlignment="Center"
                       TextWrapping="Wrap" MaxWidth="350" />
        </StackPanel>

        <!-- Progress Indicator -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8" Margin="0,16,0,0">
            <Ellipse x:Name="Dot1" Width="8" Height="8" Fill="{ThemeResource AccentFillColorDefaultBrush}" />
            <Ellipse x:Name="Dot2" Width="8" Height="8" Fill="{ThemeResource ControlStrongFillColorDisabledBrush}" />
            <Ellipse x:Name="Dot3" Width="8" Height="8" Fill="{ThemeResource ControlStrongFillColorDisabledBrush}" />
            <Ellipse x:Name="Dot4" Width="8" Height="8" Fill="{ThemeResource ControlStrongFillColorDisabledBrush}" />
            <Ellipse x:Name="Dot5" Width="8" Height="8" Fill="{ThemeResource ControlStrongFillColorDisabledBrush}" />
        </StackPanel>
    </Grid>
</ContentDialog>
